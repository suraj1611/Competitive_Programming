#include<bits/stdc++.h>
using namespace std;
#define ll long long int
#define rep(i,n) for(i=0;i<n;i++)
int main()
{
ll t;
cin>>t;
while(t--)
{
    ll n,x,y;
    cin>>n;
    vector < pair < ll,ll > >  v;
    while(n--)
    {
        cin>>x>>y;
        v.push_back(make_pair(x,y));
    }
}
}
