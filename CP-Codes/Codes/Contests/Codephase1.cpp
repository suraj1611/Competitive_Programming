#include<iostream>
#include<stdio.h>
using namespace std;
int ssum(char ch[],int l)
{
    int sum=0;
    for(i=0;i<l;i++)
        sum+=((int)char[i]);
    return sum;
}
int main()
{
    int k;
    char ch[100000000000000000000];
    gets(ch);
    int l=strlen(ch);
    cin>>k;
    int a=sum(ch,l);
    int b=sum((a*k),)
}
